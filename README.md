# Presentation of this project

This project is my first real attempt at making a Juce VST, I wanted to start simple so here it is !

# How the plug in works (for now)

I have difficulties understanding how JUCE labels works so the first slider is the "preGain" value, affecting the signal entering the soft clipper (hyperbolic tangent function), the second sliders is postGain (to correct the signal exiting the soft clipper), and there's a combo box to choose the oversampling value (n=1,2 or 4, with the oversampling value being 2^n).

<img width="205" height="243" alt="スクリーンショット 2026-09-21 15 27 18" src="https://github.com/user-attachments/assets/3312aa68-872c-4d7f-af0b-dd7c0650cbeb" />


# Implemented

- Core of the plug-in DSP treatment (applying hyperbolic tangent function to each sample of the buffer (tanh)
- Pre-gain (drive) UI linking to the DSP
- Oversampling (because there's spectral aliasing observed for high frequencies)
- APVTS State saving (to save the knobs position when the project is re-opened)
- Output gain slider to adjust the output volume

# To-do
- Link the oversampling combo box with the oversampling value (x4 by default)
- Implementing labels on the UI

# Abandonned
- "Gain-Matching" Radio button that would gain match the output level to be able to hear the harmonics generated by the waveshaping
